# A real node_modules tree: the ledger/runtime packages ship wasm that the
# Plug'n'Play loader does not resolve reliably.
nodeLinker: node-modules

enableTelemetry: false

# No dependency needs an install script; every package ships prebuilt output.
enableScripts: false
